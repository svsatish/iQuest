name: Example Tests
defaultContext: browser

tests:
  - name: Add a todo item
    tags: [smoke]
    context: browser
    steps:
      - Navigate to the TodoMVC https://demo.playwright.dev/todomvc/ page
      - Add a new todo item "Learn iQuest"
      - Verify that "Learn iQuest" appears in the todo list