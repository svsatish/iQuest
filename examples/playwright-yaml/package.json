{
  "name": "playwright-yaml-example",
  "version": "1.0.0",
  "description": "Example using iQuest with YAML-based Playwright tests",
  "type": "module",
  "scripts": {
    "generate": "npx @vsaripella/iquest generate",
    "test": "playwright test",
    "test:ui": "npx @vsaripella/iquest generate && playwright test --ui",
    "test:headed": "npx @vsaripella/iquest generate && playwright test --headed",
    "test:report": "playwright show-report"
  },
  "dependencies": {
    "@vsaripella/iquest": "file:../.."
  },
  "devDependencies": {
    "@playwright/test": "^1.57.0"
  }
}
